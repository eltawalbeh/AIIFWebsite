import { CmsSidebar } from '../../components/CmsSidebar';
import { SiteHeader } from '../../components/SiteHeader';
const checks=[['Desktop','1440px · 1280px','Typography, image crops, hover and focus states'],['Tablet','1024px · 768px','Navigation, grids, spacing and touch targets'],['Mobile','430px · 390px · 360px','Compositions, readable headings and CTA clarity'],['Arabic RTL','All public routes','Direction, language switch, long names and fallbacks'],['CMS integrity','All collections','Only approved/published records appear publicly'],['Accessibility','All public routes','Keyboard focus, contrast, labels and semantic landmarks'],['Performance','Public routes','Image size, font loading and responsive asset behaviour']];
export default function CmsQaPage(){return <div className="admin"><CmsSidebar/><main className="admin-main"><SiteHeader admin/><p className="eyebrow">CMS / Quality assurance</p><h1>Launch QA</h1><p className="lead">A controlled review checklist before public launch. This does not replace approval in the Verification Center.</p><section className="cms-section"><div className="qa-grid">{checks.map(([area,viewport,focus])=><article key={area}><p className="eyebrow">{viewport}</p><h2>{area}</h2><p>{focus}</p><label className="qa-check"><input type="checkbox"/> Checked in review</label></article>)}</div></section><section className="warning"><b>Release rule</b><p>Do not publish a record with a missing source, a missing Arabic translation where required, or a workflow status below Published.</p></section></main></div>}
