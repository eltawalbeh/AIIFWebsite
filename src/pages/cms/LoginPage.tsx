import { useState } from 'react'; import { useLocation, useNavigate } from 'react-router-dom'; import { supabase } from '../../lib/supabase';
export default function LoginPage(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [message,setMessage]=useState('');const navigate=useNavigate(),location=useLocation();async function signIn(e:React.FormEvent){e.preventDefault();if(!supabase){setMessage('Supabase connection is not configured yet. Add the publishable connection values before enabling live login.');return}const {error}=await supabase.auth.signInWithPassword({email,password});if(error){setMessage(error.message);return}navigate((location.state as {from?:string})?.from||'/cms')}return <main className="login"><section><p className="eyebrow">AIIF / PRIVATE</p><h1>Content dashboard</h1><p>For Super Admin and Content Admin users only. Public registration is not enabled.</p><form onSubmit={signIn}><label>Email<input value={email} onChange={e=>setEmail(e.target.value)} type="email" required autoComplete="email"/></label><label>Password<input value={password} onChange={e=>setPassword(e.target.value)} type="password" required autoComplete="current-password"/></label><button className="button" type="submit">Sign in</button>{message&&<p className="form-note">{message}</p>}</form></section></main>}
