import { Link, useParams } from 'react-router-dom'; import { SiteHeader } from '../../components/SiteHeader'; import { useLocale } from '../../context/LocaleContext';
const names:Record<string,string>={news:'News',press:'Press releases',awards:'Awards',publications:'Publications'};
export default function MediaListPage(){const {type='news'}=useParams();const {text}=useLocale();return <main><SiteHeader/><section className="page-hero"><p className="eyebrow">{text('Media Center','المركز الإعلامي')}</p><h1>{names[type]||text('Media Center','المركز الإعلامي')}</h1><p>{text('Content is managed in the CMS and appears here only after publication.','تُدار المواد عبر نظام إدارة المحتوى ولا تظهر هنا إلا بعد النشر.')}</p></section><section className="empty-state"><h2>{text('No published items yet.','لا توجد مواد منشورة حتى الآن.')}</h2><p>{text('Use the CMS to add English and Arabic content, an image, date, status and publication settings.','استخدم نظام إدارة المحتوى لإضافة المواد باللغتين والصورة والتاريخ وحالة النشر.')}</p><Link className="underlink" to="/media-center">{text('Back to Media Center','العودة إلى المركز الإعلامي')}</Link></section></main>}
