import type { LeadershipRecord } from '../types/cms';
import { useLocale } from '../context/LocaleContext';
export function LeadershipCard({leader}:{leader:LeadershipRecord}){const {language}=useLocale();const name=language==='ar'?(leader.fullNameAR||leader.fullNameEN):leader.fullNameEN;const position=language==='ar'?(leader.positionAR||leader.positionEN):leader.positionEN;return <article className="leader-card">{leader.photo?<img className="leader-photo" src={leader.photo} alt={name}/>:<div className="portrait-placeholder" aria-label="Official portrait pending"><span>AIIF</span><small>{language==='ar'?'الصورة الرسمية قيد الانتظار':'Official portrait pending'}</small></div>}<div><p className="eyebrow">{leader.leadershipRole==='chairman'?(language==='ar'?'رئيس مجلس الإدارة':'Chairman'):leader.leadershipRole==='ceo'?(language==='ar'?'الرئيس التنفيذي':'Chief Executive Officer'):(language==='ar'?'مجلس الإدارة':'Board')}</p><h3>{name}</h3><p>{position}<br/>{leader.country}</p></div></article>}
