# AIIF Division 4 — Figma Make upload

Upload this update into the existing AIIF Figma Make project. It contains only supported application files.

Do not upload SQL, migration, `.env`, or database files to Figma Make.

The AIIF Supabase database update has already been applied. This package adds the public Media Center and Contact Us routes, plus their CMS screens.

Keep every existing file that is not included in this update.
